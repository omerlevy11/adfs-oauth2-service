export interface BiopassClaims {
    upn: string;
    email: string;
    uniqueName: string;
    phoneNumber: string;
}

export interface Claims {
    upn: string;
    email: string;
    uniqueName: string;
    phoneNumber: string;
    exp:number;
}